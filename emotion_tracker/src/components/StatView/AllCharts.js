// 3. 4. ja 5.
// tekee valinnan mikä chartti näkyy sekä mitä chartissa näkyy. 

const AllCharts = ({
  hourRange,
  minHour,
  maxHour,
  chartDate,
  setChartDate,
  timeUnit,
}) => {
  return;
};

export default AllCharts; 